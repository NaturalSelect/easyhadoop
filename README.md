# easyhadoop

A easy way to use hadoop and spark in docker-compose

## Usage

Launch cluster:

```bash
make up
```

Stop cluster:

```bash
make down
```

Enter spark:

```bash
make spark
```

Enter hadoop:

```bash
make hadoop
```